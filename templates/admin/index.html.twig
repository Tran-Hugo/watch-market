{% extends 'base.html.twig' %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="mt-5 w-100 h-100 d-flex justify-content-start align-items-center flex-column">
    <h1>Gestion :</h1>
        <a href="{{path('app_article_index')}}">Articles</a>
        <a href="{{path('app_categorie_index')}}">Catégories</a>
        <a href="{{path('app_fournisseur_index')}}">Fournisseurs</a>
        <a href="{{path('orders_admin')}}">Commandes</a>
</div>
{% endblock %}
